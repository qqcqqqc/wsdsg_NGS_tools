# ==============================================================================
# 跨平台 NGS & CRISPR 扩增子分析小工具 - 全套环境依赖与配置指南
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Windows 宿主机环境 (用于运行 GUI 界面)
# ------------------------------------------------------------------------------
# 在 Windows 命令行 / PowerShell 的 conda 环境中运行:
# pip install -r requirements.txt

PySide6>=6.5.0
openpyxl>=3.1.0
pandas>=2.0.0


# ------------------------------------------------------------------------------
# 2. Windows WSL2 / Linux 生信计算后端安装指南 (用于后台计算)
# ------------------------------------------------------------------------------
# 在 Windows 终端中输入 wsl 进入 WSL2 终端，然后选择以下任一方式安装：
#
# 【推荐方式 A：使用 Conda (自动配齐 C++ 对齐依赖，最省心)】
#  如果 WSL 内尚无 Conda，按顺序粘贴以下 3 行即可完成安装：
#  1) curl -fsSL https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -o miniconda.sh && bash miniconda.sh -b -p $HOME/miniconda3
#  2) $HOME/miniconda3/bin/conda init bash && source ~/.bashrc
#  3) conda install -c bioconda crispresso2 cutadapt -y
#
# 【方式 B：使用原生 Python3 / pip】
#  1) sudo apt update && sudo apt install -y python3-pip python3-dev build-essential bowtie2 samtools flash
#  2) pip3 install crispresso2 cutadapt
# ==============================================================================
